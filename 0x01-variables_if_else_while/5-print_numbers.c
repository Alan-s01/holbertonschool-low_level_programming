#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - Prints numbers
 */

int main(void)
{
	int num;
	for (num = 0; num < 10; num++)
	{
		printf("%d", num);
	}
	printf("\n");
	return(0);
}

