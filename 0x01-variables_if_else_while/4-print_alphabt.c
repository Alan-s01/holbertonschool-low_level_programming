#include <stdio.h>

/**
 * Alphabets - prints the aplhabets
 */

int main(void)
{
	char letter;
	for (letter = 'a'; letter <= 'z'; letter++)
		if (letter == 'e' || letter == 'q')
		{

		}
		else
		{
			putchar(letter);
		}
	return(0);
}
